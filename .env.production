# Baked into the production browser bundle at `next build` time.
# Server deploys should also set these via .env / docker build --build-arg
# (see scripts/refresh-daemon.sh). Values here apply when build args are absent.
NEXT_PUBLIC_APP_NAME=Luna POS
NEXT_PUBLIC_API_URL=https://pos-api.cymonevo.com
